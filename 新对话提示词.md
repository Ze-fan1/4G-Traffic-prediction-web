# 新对话提示词

## 项目路径
`c:\Users\Admin\Desktop\benchmark`

## 环境
- **Python**: `E:\Software\Anaconda3\envs\ETP-exp1\python.exe`（PyTorch 2.10+cu130, RTX 5050）
- **Node**: 系统全局
- **base环境 PyTorch 损坏**，必须用 ETP-exp1

## 启动命令
```bash
# 终端1: 后端
cd c:\Users\Admin\Desktop\benchmark\backend
E:\Software\Anaconda3\envs\ETP-exp1\python.exe server.py      # → localhost:8000

# 终端2: 前端
cd c:\Users\Admin\Desktop\benchmark\react-app
npm run dev                                                    # → localhost:5173/4G-Traffic-prediction-web/

# 关闭后端
netstat -ano | findstr ":8000" && taskkill //PID <PID> //F

# 构建并部署
cd react-app && npm run build
cp -r dist/* ../docs/
git add . && git commit -m "..." && git push origin master
```

## 网站与部署

### 三层架构
```
用户浏览器
  ↓ 访问 GitHub Pages
前端静态页面（React → docs/）
  ↓ 调 API（VITE_API_URL）
Cloudflare Tunnel（内网穿透）
  ↓ 转发
本机 FastAPI（localhost:8000）
  ↓ 调模型
PyTorch / XGBoost / statsmodels 推理
```

### 地址
- **本地前端**: `http://localhost:5173/4G-Traffic-prediction-web/`
- **生产前端**: `https://ze-fan1.github.io/4G-Traffic-prediction-web/`
- **本地后端**: `http://localhost:8000`
- **GitHub 仓库**: `https://github.com/Ze-fan1/4G-Traffic-prediction-web`

### 部署方式
- GitHub Actions → `docs/` 目录 → GitHub Pages（自动，push 后 1-2 分钟生效）
- **生产 API 地址**: 在 `react-app/.env.production` 中设置 `VITE_API_URL`

### Cloudflare Tunnel（后端公网暴露）
```bash
cloudflared.exe tunnel --url http://localhost:8000
```
- 当前隧道 URL: `https://varieties-climb-vip-genesis.trycloudflare.com/api`
- ⚠️ trycloudflare URL 每次重启隧道会变，需更新 `.env.production` + `npm run build` + `cp -r dist/* ../docs/` + 重新 push

### Git Push
- **HTTPS**（直连）: `git push origin master`，如果被墙加 `GIT_SSL_NO_VERIFY=1` 前缀
- **SSH**（备选）: `git remote set-url origin git@github.com:Ze-fan1/4G-Traffic-prediction-web.git`
- SSH 公钥（`~/.ssh/id_ed25519.pub`）:
  ```
  ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIA7rqerc+HRB1fynJlya6H2YWMA9FBfJpMsN/MNYg0oT
  ```
  需在 https://github.com/settings/keys 添加到 Ze-fan1 账号

## 后端架构 (FastAPI)

### API 清单（9个）
| 端点 | 用途 |
|------|------|
| `/api/health` | 健康检查（GPU状态、已加载模型、活跃job数） |
| `/api/models` | 列出全部 23 个模型元数据 |
| `/api/preset-curves/{model}?window=4394` | 预置 benchmark 曲线（σ空间） |
| `/api/preset-models` | 列出有预置曲线的模型名 |
| `/api/run/{model}` | 统一运行入口（训练/推理），返回 job_id |
| `/api/predict/{model}` | 自定义数据上传预测（CSV/Excel/Parquet） |
| `/api/job/{id}` | 查询 job 进度（500ms 轮询） |
| `/api/parse-file` | 解析上传文件，返回列名和预览 |
| `/api/unload` | 手动卸载模型释放显存 |

### 核心文件
| 文件 | 职责 |
|------|------|
| `server.py` | FastAPI 入口，路由 + job 管理 + 全局异常处理 |
| `model_registry.py` | 23 模型注册表（类型、checkpoint 路径、result_dir、Tier等级） |
| `model_loader.py` | PyTorch/HuggingFace 按需加载，全局单例 + 线程锁 |
| `inference_engine.py` | 统一推理分发（statistical/PyTorch/HuggingFace/XGBoost 四类） |
| `data_pipeline.py` | CSV/Excel/Parquet 解析 + StandardScaler + 滑动窗口 + 通道对齐 |
| `train_runner.py` | DL 从零训练子进程（10 epochs → 全部5378窗口预测 → σ空间曲线#4394） |
| `preset_curves.py` | 预置 benchmark 曲线加载（自动检测原始/σ空间，统一输出σ） |
| `generate_stat_presets.py` | 一次性生成/修复统计模型的 pred.npy（AutoAR、LinearRegression） |

## 数据集

### 文件
| 文件 | 行数 | 用途 |
|------|------|------|
| `df_4g_train_100.parquet` | 123,313 | 训练集，用于拟合 StandardScaler |
| `df_4g_test_100.parquet` | 16,181 | 测试集，用于 benchmark 评估和预置曲线 |
| `df_4g_base_100.parquet` | 16,181 | train_runner 的训练数据（与 test 行数相同但内容不同） |

### 滑动窗口协议
- 输入 24h × 8通道 → 输出 24h × 8通道
- 步长 3h，共生成 **5,378 个测试窗口**
- 默认展示窗口 **#4394**（秩相关最优代表性窗口）

### 8 特征通道
```
erab流量, pdcch利用率, pdsch利用率, pusch利用率,
上行流量, 下行流量, 总流量, 有效连接数
```

## 23个预置模型（run_type 分布）

| run_type | 数量 | 模型 |
|---|---|---|
| `train_dl` | 12 | DLinear, LightTS, TSMixer, PatchTST, iTransformer, Autoformer, Transformer, Informer, TimesNet, SCINet, SegRNN, ★ BaseModel |
| `inference_stat` | 6 | Naive, Persistent 24h, Historical Avg, AutoARIMA, AutoAR, LinearRegression |
| `inference_pretrained` | 2 | IBM TTM, Chronos2 |
| `train_xgboost` | 1 | XGBoost |
| `train_mamba` | 1 | Mamba |
| `train_timellm` | 1 | TimeLLM |

### 预置曲线覆盖情况
- 全部 23 个模型都有 `pred.npy` → 都能在"数据验证"中查看预置 benchmark 曲线
- AutoAR 和 LinearRegression 的 `pred.npy` 由 `generate_stat_presets.py` 生成（2026-07-15），σ空间，两者算法相同（时间序号线性回归）
- 各模型的 `pred.npy` 空间不一致（有的 σ 有的原始），`preset_curves.py` 通过 `mean > 5.0` 自动检测并统一转换

### 模型类别分布
| 类别 | 模型 |
|------|------|
| Statistical | Naive, Persistent 24h, Historical Avg, AutoARIMA, AutoAR, LinearRegression |
| Tree | XGBoost |
| Baseline | ★ BaseModel |
| Transformer | PatchTST, iTransformer, Autoformer, Transformer, Informer |
| MLP | DLinear, LightTS, TSMixer, IBM TTM |
| CNN | TimesNet, SCINet |
| RNN | SegRNN |
| SSM | Mamba |
| LLM | TimeLLM, Chronos2 |

## 坐标系
- **σ空间**: 所有曲线 StandardScaler 标准化（减均值 ÷ 标准差）
  - 正值 = 高于历史均值，负值 = 低于历史均值
  - 统一不同量纲通道到同一尺度公平比较
- **默认窗口 #4394**，前端可选 #0 / #100 / #4394
- 真实值始终从 `df_4g_test_100.parquet` 实时计算，所有模型共享，保证一致

## 曲线图样式（全黑学术风格）

| 元素 | 线型 | 颜色 | 图例 |
|------|------|------|------|
| 真实值 | 虚线 `[6, 3]` | `#18181B` 2px | "真实值" |
| 本地预测值 | **实线** | `#18181B` 2px | `"{模型名}本地预测值"` |
| 训练结果 #1 | 实线 | `#18181B` 1.8px | `"✅ 训练结果 #1 · 窗口#4394/5378"` |
| 训练结果 #2 | 长虚线 `[6, 3]` | `#18181B` 1.8px | `"✅ 训练结果 #2 · …"` |
| 训练结果 #3 | 短虚线 `[2, 2]` | `#18181B` 1.8px | … |
| 训练结果 #4 | 点划线 | `#18181B` 1.8px | … |
| 训练结果 #5 | 点线 | `#18181B` 1.8px | … |

## 前端页面结构

| 页面 | 路由 | 功能 |
|------|------|------|
| OverviewPage | `/` | 首页 Hero + ACC 排名 + 核心发现 |
| PerformancePage | `/performance` | ACC 横向对比 + 雷达图 + 散点图 + 类别汇总 |
| DetailsPage | `/details` | 数据集说明 + 实验设置 + 类别汇总 + TOP20 + 深度分析 |
| CurvesPage | `/curves` | 多模型预测曲线叠加对比（静态 benchmark 数据） |
| **模型中心** | (DemoPanel) | 单模型数据验证：预置曲线 + 从零训练 + 多次叠加 |

## 已实现的特色功能
- 预置 benchmark 曲线自动加载（**23模型**，σ空间统一，`preset_curves.py` 自动检测空间并转换）
- **切换卡片记忆**: 训练状态 + 上传预测结果全部全局缓存（`modelHistoryCache` / `modelRunState`），切模型再回来不丢失
- **后台训练**: 切换窗口/模型时训练继续在后台跑（`globalPollers` Map 管理，`startGlobalPoll` / `stopGlobalPoll`）
- **自定义数据上传预测**: 支持 CSV/Excel/Parquet，用户列自动对齐到8模型通道
- **多模型叠加对比**: UploadPanel 同一张图上叠加所有模型的预测线，Y轴统一
- **逐个删除预测**: 每条预测线可单独 × 删除
- 多次训练图表叠加（真实值=虚线，本地预测值=实线，训练结果=不同线型区分次数）
- 轮询间隔 500ms，进度条按 epoch
- σ空间说明 Banner（灰底，解释正值/负值含义）

## 自定义预测通道对齐
用户上传 CSV 列数 ≠ 模型8通道时：
1. 列名匹配 `FEATURE_COLS` 的 → 映射到对应通道
2. 未匹配的空通道 → 循环填充 + 微小噪声（防止全零导致 SCINet 退化）
3. HuggingFace 预训练模型（IBM TTM/Chronos2）→ 直接用原始空间，跳过标准化
4. **不适配自定义预测的模型**: Mamba/TimeLLM（独立脚本不保存权重）、SCINet（二叉树架构退化）

## 首页文案
已删除所有"自研多尺度特征融合架构""已非 SOTA"等不当表述。

## 关键修复记录

### 2026-07-15（本轮）
- **AutoAR + LinearRegression 预置曲线**: `generate_stat_presets.py` 生成 σ空间 pred.npy，`model_registry.py` 添加 result_dir
- **AutoAR 指标修正**: `models.js` 中 AutoAR 的 MSE 从 3.3104 更正为 4.2247（与 LinearRegression 一致，两者算法相同）
- **图表样式修改**: 真实值从实线→虚线 `[6,3]`，预置图例从 `"📋 {model} 预置"` → `"{model}本地预测值"`（实线）
- **从零训练重复结果 bug**: `DemoPanel.jsx` 中模型切换 useEffect 和 handleRun 各有一套完成处理逻辑，导致一次训练产生两条记录。修复方案：
  - 提取共享 `buildDoneEntry()` 函数
  - 模型切换 useEffect 改为一次性状态查询（不再持续轮询）
  - 添加 useEffect cleanup 自动停掉旧模型 poller
  - handleRun 开始前清除残留 poller
- **模型总数**: 21 → 23（新增 AutoAR、LinearRegression 预置曲线）

### 历史修复
- `total_epochs` 初始值 = 0（从零开始），TRAIN_START 行输出后自动更新为 10
- `DemoPanel.jsx` — 轮询用 `API_BASE` 而非硬编码 `/api`（修复生产环境进度永远 0%）
- `PlaygroundPage.jsx` — `API_BASE` 使用 `import.meta.env.VITE_API_URL` 而非硬编码 `/api`
- 通道名小写匹配、σ空间统一、真实值统一计算、窗口#4394统一
- 后端 `_jobs` 定期清理（1小时过期）、全局异常处理防 HTTP 500
- `model_loader.py` 线程锁防并发加载/卸载冲突
- `os.chdir()` 在 `model_loader.py` 模块级执行（TSL 库需要）
- `preset_curves.py` 默认窗口 #4394、自动检测原始/σ空间（`abs(mean) > 5.0` 判定为原始空间）
- 前端 API_BASE 使用 `import.meta.env.VITE_API_URL || '/api'` 环境变量
- Naive 模型预测为水平直线（正确行为：复制最后一个输入值）
- `GIT_SSL_NO_VERIFY=1 git push` 解决 GitHub HTTPS 被墙问题

## 已知限制
- Mamba/TimeLLM 训练脚本不保存模型权重 → 只能 benchmark 训练，不能自定义预测
- SCINet 二叉树架构对输入通道极其敏感 → 非8通道真实4G数据退化为恒值
- BaseModel checkpoint 不存在于 `checkpoints/` 目录（`External_BaseModel_4G_Base_v2_Verify` 缺失）→ 仅预置曲线和从零训练可用，不能用于自定义预测
- `model_loader.py` 单例缓存，同一时间只能加载一个 PyTorch 模型
- `trycloudflare.com` URL 重启隧道后变化，需重新构建前端
- GitHub Pages 在国内可能被墙，需要代理访问
- AutoAR 和 LinearRegression 算法完全相同（时间序号线性回归），预测结果一致
- `train_runner.py` 训练数据为 `df_4g_base_100.parquet`，与 benchmark 的 `df_4g_test_100.parquet` 不同 → 从零训练结果与预置曲线数值不同是正常现象
